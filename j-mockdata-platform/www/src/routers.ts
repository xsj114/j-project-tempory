import Login from './views/Login'
import Create from './views/Create'

const routes = [
    {
        path: '/login',
        component: Login
    },
    {
        path: '/create',
        component: Create
    }
]

export default routes
